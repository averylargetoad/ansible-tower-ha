# Ansible Tower Settings
# Generated by Ansible

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '{{ db_name }}',
        'USER': '{{ db_user }}',
        'PASSWORD': '{{ vault_tower_pg_password }}',
        'HOST': '{{ db_host }}',
        'PORT': '{{ db_port }}',
    }
}

# Cluster configuration
CLUSTER_HOST_ID = "{{ ansible_hostname }}"

# Redis cache configuration
CACHES = {
    'default': {
        'BACKEND': 'django_redis.cache.RedisCache',
        'LOCATION': 'redis://{{ redis_host }}:{{ redis_port }}/0',
        'OPTIONS': {
            'CLIENT_CLASS': 'django_redis.client.DefaultClient',
        }
    }
}

# Session configuration
SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
SESSION_CACHE_ALIAS = 'default'

# Celery/RabbitMQ configuration
BROKER_URL = 'amqp://{{ rabbitmq_user }}:{{ vault_rabbitmq_password }}@{{ rabbitmq_host }}:{{ rabbitmq_port }}/{{ rabbitmq_vhost }}'
