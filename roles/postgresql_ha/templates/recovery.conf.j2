# PostgreSQL Recovery Configuration for Standby
# Generated by Ansible

standby_mode = 'on'
primary_conninfo = 'host={{ hostvars[groups['database_cluster'][0]].ansible_host }} port={{ db_port }} user=replicator password={{ vault_pg_replication_password }}'
trigger_file = '/tmp/postgresql.trigger.{{ postgresql_version }}'
restore_command = ''
